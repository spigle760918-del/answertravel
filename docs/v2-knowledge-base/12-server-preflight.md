# 阿里云现有业务服务器只读核查

日期：2026-09-13。状态：已查看宝塔及部分终端输出；未安装、部署、重启或修改服务器服务。

## F1 观察事实

- 用户已在独立浏览器中自行登录宝塔；可读取宝塔页面。
- x86_64，宝塔显示 Alibaba 3、面板 11.8.1。
- 2 个运行中的 PHP 站点；Nginx 1.30.3、PHP 7.2；数据库概览 2 个。未查看数据库密码或业务表。
- `free -m` 当次输出：总内存 7462 MiB，available 4770 MiB；1 GiB swap 已使用。页面的“已用内存”与 Linux available 口径不同。
- 系统盘约 99 GiB，已用 48 GiB，可用 47 GiB。
- 现有监听包含 80、443、22、888、3306、宝塔端口及回环邮件端口。
- Nginx/MySQL 显示 active；Docker/Redis/PostgreSQL 显示 inactive，PATH 未发现对应可执行文件。
- 宝塔 Docker 页显示“立即安装”，未点击。

## F3 判断与后续选择

资源快照支持考虑轻量隔离测试，但 swap 已占满，需要限额运行，不能只凭首页百分比断言无影响。

优先核实原生临时测试方案：独立 `/var/tmp/answertravel-v2-verify-*/v2` 副本，Node.js 官方便携包、固定 PostgreSQL 开发依赖、官方 Redis 源码；只以非特权账号运行、不安装全局服务、不监听公网端口、不触碰网站目录或已有数据库。

用户协助的终端输出已在页面核实：`gcc`、`make`、`runuser`、`curl`、`tar` 存在，glibc 2.32，`www` 为 uid/gid 1000 的既有账号。准备使用独立临时 systemd 单元限制为 CPU 50%（半核）、内存 1.5 GiB、最长 20 分钟，可写范围仅测试目录，并隐藏 `/www` 现有网站目录；未执行。

宝塔终端反复重新连接导致部分输入未执行；用户协助完成了一条只读工具检查。这是控制通道稳定性问题，不代表 V2 编译/测试失败。

原生准备及启动脚本均已落地，可供审阅；准备/测试在非特权账号下运行。脚本仅创建一次性测试目录和临时单元，不写入系统安装目录、不新增登录账号、不改端口和防火墙，不让现有 MySQL 参与测试。

## 边界

- Windows 的 33 项测试与两项恢复演练保持已验证。
- Linux 原生准备脚本已编写，待实机验证；不把脚本存在当作 Linux 通过。
- 不安装 Docker、不更改防火墙、不修改现有网站、数据库、PHP、Nginx 或计划任务。
- 不把面板显示的“无备份”直接断言为不存在其他备份；未扩展处理旧站维护事项。

## 实机验证结果（2026-09-13 16:30）

- 已通过宝塔文件管理器上传审阅后的验证包到 `/tmp`；远端 SHA-256 为 `735e801ee39c2e18aae737c02ef8e3948c97d86a9dd8c89da619c84f37a17d0d`，与本地一致。
- 尝试启动一次性 `systemd-run` 隔离单元时，宝塔/主机安全层拒绝最小 shell 探针：`systemd-run` 返回 `status=203/EXEC`，完整尝试被终止为 `signal=SEGV`。这不是 V2 测试断言失败，也不是业务服务异常。
- 尝试用既有 `www` 用户直接运行时，安全层对 `runuser`、`su` 及子进程返回 `Your request has been recorded. Tips from BT security !!!`，导致脚本在进入下载/安装前被阻断。未绕过该安全策略。
- 验证后的只读检查显示 `nginx=active`、`mysql=active`，80、443、3306、41927 监听仍在；未重启、安装或改动这些服务。
- 服务器实机 Linux 验证结论：`blocked-by-host-policy`，不得把它记为阿里云原生验证通过。此后同一 V2 提交已通过 GitHub Ubuntu CI；Windows 本地仍为 `33 passed / 0 failed / 0 skipped`，两类证据分别记录。

## 保留证据与清理边界

## 追加只读预检（2026-09-15 17:35）

- Alibaba Cloud Linux 3.2104 U13.1，x86_64，内核 5.10.134-19.5.al8。
- `MemAvailable` 约 4659 MiB，swap 1024 MiB 已全部使用；根分区 99 GiB、可用约 45 GiB。
- 80、443、22、888、3306、41927 等端口已有监听；预发布不得占用这些端口。
- Docker 未安装；未执行安装。
- 以上仍是只读事实，不改变此前 `blocked-by-host-policy` 的原生验证结论。

- 远端验证副本保留在 `/var/tmp/answertravel-v2-verify-ZMdC2p4B` 及 `/var/tmp/answertravel-v2-verify-native2`，仅含验证包内容和失败现场，未写入网站目录。
- `/tmp` 中的审阅包和临时启动脚本属于本次验证产物；后续如需清理，须先再次确认精确路径，不执行宽范围删除。
