# 意图卡：北京珈程生产首次真实观察执行门禁 V1

状态：生产恢复补采与公网验收台展示修正已完成；39条成功、1条截断失败，Gate E 待用户确认
日期：2026-09-17

## 用户价值

把已经在本地验收过的首次真实观察契约安全地落到阿里云生产实例。默认只预演并展示将要执行的范围；只有用户对真实费用门禁作出一次明确确认后，才创建生产观察计划并把任务放入持久队列。

## 固定范围

- 租户：北京珈程国际旅行社生产租户 `09f3c859-9f60-4370-8364-a55229632edd`；
- 问题：生产库最新已批准的 20 条不可变问题，不重新生成、不改写；
- 轮次：每题 2 轮，共 40 个计划样本；
- Provider：仅 DeepSeek API，模型请求名 `deepseek-chat`；
- 参数：简体中文、中国大陆游客计划北京旅行、temperature 0.2、单回答最多 1000 tokens、60 秒超时、最多 3 次尝试；
- 预算：计划总 Token 上限 60,000，达到预算门禁后停止剩余任务；
- 输出：不可变尝试、原始回答、引用扫描和基础 GEO 分析；
- 不建立周期监测，不生成竞品直问，不生成或发布内容。

## 执行门禁

1. 脚本默认只能运行 `dry-run`，不得写数据库或入队；
2. 真实执行必须同时提供 `--execute` 与固定授权标记；
3. 生产租户、已批准问题数、采样规则、计划样本数和 Token 上限任一不匹配即停止；
4. 首次执行前，生产观察计划、原始回答、周期计划和观察队列待处理任务必须为 0；
5. 计划 ID、周期键和 40 个目标的幂等键固定；中断后只允许对同一计划安全重跑，不允许静默创建第二个基线；
6. 不读取或输出 DeepSeek Key、数据库密码或完整连接串；
7. 真实入队前再次核对 Worker 已使用生产密钥且运行正常。

## 失败与停止边界

- 检测到未知观察计划、已有其他周期、已有非本计划回答或队列中存在无关待处理任务时停止；
- 密钥失效、余额/限额异常、连续 Provider 故障、超时、截断或空正文均保留失败证据；
- DeepSeek API 结果不得描述为 DeepSeek Web/App、联网搜索或其他模型表现；
- 邮件外部投递尚未验收，不把告警标记为就绪；本次执行依赖数据库、队列、Worker 日志和验收台观察状态。

## 当前授权状态

用户已授权进入本阶段的准备与验证，但尚未授权生产真实 API 调用。准备完成后，只请求一次真实费用确认：

`20 条问题 × 2 轮，最多 40 次 DeepSeek API 调用，计划 Token 硬上限 60,000。`

## 本地实施证据

- 新增生产入口 `dist/scripts/create-jiacheng-production-baseline.js`，默认只输出 dry-run 摘要；
- 真实执行同时要求 `--execute` 与固定授权标记；
- 固定生产租户、问题快照、模型、轮次、参数、计划 ID、周期键和40个幂等目标；
- 对未知观察计划、非本基线回答、周期计划、无关队列任务和问题快照漂移执行 fail-closed；
- Node.js 24.21.0 下类型检查、Web类型检查、生产Web构建、103项非集成测试和生产服务构建通过；41项依赖数据库/Redis的集成测试按环境设计跳过；
- 本地验证没有连接阿里云生产数据库或Redis，没有写生产数据、入队或调用DeepSeek。

## 生产 dry-run 证据

- 生产租户：`09f3c859-9f60-4370-8364-a55229632edd`；
- 已批准问题组版本：2，共20条；
- 计划范围：2轮、40个样本、`deepseek-chat`、API终端、60,000 Token计划上限；
- 当前生产状态：观察计划0、原始回答0、观察队列待处理任务0；
- 输出：`deepseekCalled=false`、`executionAuthorized=false`；
- dry-run 未写数据库、未入队、未重启生产API/Worker、未产生DeepSeek费用。

## 真实费用授权

2026-09-17，用户明确回复：`YES，授权北京珈程生产首次真实观察：最多40次DeepSeek API调用，60000 Token上限`。

该授权仅允许创建本意图卡固定的单次生产基线并入队40个幂等目标。它不授权增加预算、创建周期计划、竞品直问、多模型扩张、内容生成、官网修改或发布。

## 首次执行失败证据

- 40个目标已按授权入队，但全部在调用Provider前发生网络失败；
- 每个目标保留3次`network_error`尝试，共120条失败证据；成功回答0、使用Token 0、引用扫描0、GEO分析0；
- 根因已独立复现：`aliyun_answertravel`为刻意设置的内部网络，Worker仅连接该网络时DNS请求返回`EAI_AGAIN`；相同镜像连接`answertravel_proxy`访问DeepSeek入口返回HTTP 401，证明公网路径可达；
- 修复方案是Worker同时连接内部数据网络和非内部代理网络，不把`aliyun_answertravel`改为外部网络，不让PostgreSQL或Redis获得公网出站；
- 失败计划与120条尝试保持不可变，不能删除或伪装为未执行。网络修复后是否创建恢复计划需单独遵循失败补采门禁。

## Worker 出站修复结果

- 生产Compose已将Worker同时连接`aliyun_answertravel`与`answertravel_proxy`；
- Worker重建后为`running`、重启次数0，不发布宿主机端口；
- Worker访问DeepSeek入口返回HTTP 401，证明DNS、TLS和公网路径已经可达；
- API、PostgreSQL、Redis及内部网络配置未改变；
- 原失败任务未重新执行，成功回答和Token仍为0。

## 网络故障恢复补采授权

2026-09-17，用户明确回复：`YES，授权网络故障恢复补采：同一20题×2轮，最多40次DeepSeek API请求，每目标仅1次，60000 Token上限`。

恢复补采必须使用独立固定计划和新幂等键，并在创建前验证原计划恰好包含40个目标、120条`network_error`、0回答、0 Token且观察队列无待处理任务。每个恢复目标只允许1次尝试，最多40个Provider请求；不删除原失败证据，不开启周期监测或发布。

恢复计划生产dry-run已通过：原失败计划ID为`e72876fe-076c-489a-9a66-82b2c2855982`，恢复计划ID为`8cb46184-c355-4a36-b744-ac00f9148dec`；确认120条网络失败、0 Token、0回答、40个恢复目标、每目标1次、队列待处理任务0，输出`deepseekCalled=false`与`executionAuthorized=false`。

## 网络故障恢复补采结果

- 恢复计划已按独立授权入队40个目标，每个目标最多1次Provider请求；
- 最终处理40个目标：39个成功、1个`incomplete_response`终止失败、0个可重试失败；
- 成功保存39条不可变原始回答，实际使用17,436 Token，未超过60,000 Token上限；
- 39条成功回答均已完成引用扫描和基础GEO分析；当前诊断快照共21条；
- 中性自然样本21条，品牌自然提及0，明确排名0；当前诊断继续如实返回证据不足，不据此生成或发布内容；
- 原失败计划的120条`network_error`尝试保持不可变，恢复计划的1条截断失败也保持为失败证据；
- BullMQ观察队列`failed=41`由原网络故障40个任务与恢复计划1个截断任务组成，所有队列的`wait/active/delayed/prioritized`均为0；
- 公网首页和`/health/ready`均返回HTTP 200，Worker保持`running`且重启次数0；
- 未建立周期监测，未启动竞品直问、多模型扩张、内容生成、官网修改或发布。

## 收尾展示修正

生产数据已证明旧限制文案“尚未创建观察计划或采集任何答案”失效；验收台必须改为按真实计划数与成功回答数动态显示，并保留失败证据说明。阿里云生产备份与隔离恢复演练已经通过，但只能在当前实例配置明确证据后把`cloud_backup`标为`ready`。邮件外部告警和周期监测继续保持`pending`。

2026-09-17，修正已部署到阿里云生产API。API健康状态为`healthy`、重启次数0、固定代理地址仍为`172.30.0.10`，公网首页和就绪接口均返回HTTP 200。公网浏览器只读检查确认：39条有效回答、17,436 Token、云端备份与恢复为`ready`；云端日志告警与周期监测仍为`pending`；能力边界已改为2个观察计划、39条成功回答并明确保留失败尝试。未新增Provider调用或周期任务。
