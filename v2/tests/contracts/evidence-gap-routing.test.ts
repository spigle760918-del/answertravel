import{randomUUID}from"node:crypto";import{describe,expect,it}from"vitest";import{routeEvidenceGaps,type GapInput}from"../../src/modules/evidence-gap-routing/evidence-gap-routing.js";
const f=(question:string,claimText:string,cycleKey="daily-1",panelRole="baseline"):GapInput=>({findingId:randomUUID(),verdict:"insufficient_evidence",question,claimText,cycleKey,panelRole});
describe("evidence gap routing",()=>{it("groups repeated claims without treating occurrences as unique evidence",()=>{const a=f("退款政策是怎样的？","无法查询退款条款","baseline"),b={...f("退款政策是怎样的？","无法查询退款条款","daily-2"),claimText:a.claimText};const x=routeEvidenceGaps(randomUUID(),[a,b]);expect(x.clusters[0]).toMatchObject({theme:"refund_cancellation",occurrenceCount:2,uniqueClaimCount:1,cycleCount:2,recommendedRoute:"product_service",contentBriefEligible:false});});
it("routes product facts to the correct actions",()=>{const x=routeEvidenceGaps(randomUUID(),[f("5天4晚价格是多少？","无法提供具体价格"),f("是否适合老人或小孩？","无法判断步行强度"),f("有许可证吗？","无法核实许可证")]);expect(new Set(x.clusters.map(c=>c.recommendedRoute))).toEqual(new Set(["website_structure","brand_truth","external_source"]));});
it("does not create content briefs merely because gaps are frequent",()=>{const x=routeEvidenceGaps(randomUUID(),Array.from({length:10},(_,i)=>f("纯玩承诺可靠吗？",`需要核验合同条款${i}`)));expect(x.clusters[0]?.contentBriefEligible).toBe(false);});});
