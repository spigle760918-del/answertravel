# 品牌真相中心意图卡：冲突、缺口与公开范围

状态：Gate A/B/C/D 已通过，待 Gate E 业务验收
日期：2026-09-14

## 用户问题

文旅品牌资料往往来自负责人补充、官网、产品说明和历史文档，同一事实可能互相冲突、缺少关键颗粒度，或不适合公开。系统需要在事实进入问题智能、内容生成和发布路径之前，自动指出风险并把人工判断压缩为少量 Yes/No。

## 本切片输入

- 已版本化的品牌事实草稿和已审核版本；
- 每条事实的字段、陈述、来源说明、事实等级和审核状态；
- 品牌别名、产品服务、差异化能力、禁用表达和公开范围；
- 系统维护的文旅品牌最小完整度规则版本。

## 本切片输出

- 冲突候选：指出互不相容的事实、涉及字段、证据和判断理由；
- 缺口候选：按品牌主体、产品、服务、差异化、保障和适用人群列出缺失信息；
- 公开范围建议：`public`、`internal`、`restricted` 或 `undetermined`；
- 最小人工确认队列：合并相同问题，仅要求确认事实取舍、补充真相或公开范围；
- 版本化质量报告和完整审计记录。

## AI 与人的边界

- AI 自动检测重复、矛盾、颗粒度不足、来源薄弱、禁用表达命中和公开范围风险；
- AI 可以提出建议和追问，但不得自行把新信息写成已确认事实；
- 人只确认真实取值、必要补充和公开范围；
- `undetermined`、存在未解决冲突或关键缺口的事实，不得进入生产问题、内容或发布路径；
- 本切片不调用外部模型，不抓取官网，只处理品牌真相中心内已有资料。

## 自动化等级

- A0：自动扫描、分组、记录证据和风险；
- A1：自动建议冲突取舍、缺口优先级和公开范围；
- A2：自动生成最小追问清单；
- 最终事实取值与公开范围仍由人 Yes/No 或补充真实信息。

## 验收场景

1. 正常：两条产品事实发生明确冲突，系统保留原文和来源，生成一个合并确认项，人工确认后产生新版本。
2. 边界：只是措辞不同但语义一致时标记为可能重复，不误判为确定冲突。
3. 缺口：缺少适用人群、价格时效或服务边界时，给出具体补充问题，不生成虚构答案。
4. 公开范围：敏感联系方式、内部价格或未公开能力默认不进入公开路径；无法判断时返回 `undetermined`。
5. 失败：跨租户读取、覆盖历史报告、绕过人工确认或把建议直接升级为事实均被拒绝并审计。

## 非目标

- 不导入用户真实品牌资料；
- 不调用 DeepSeek 或其他外部 AI；
- 不抓取官网或第三方页面；
- 不生成游客问题、文章或 GEO 指标；
- 不发布任何对外内容。

## Gate A 后的实施顺序

用户确认本意图卡后，先完成版本化契约与正常/边界/失败样例，再实现数据库、服务、审计和测试的最小真实垂直切片。未经确认不得进入编码。

## Gate A 确认

- 2026-09-14，用户明确回复 `YES`，同意按本意图卡进入实现。

## Gate B/C 实现证据

- `v2/src/modules/brand-truth/brand-truth-quality.ts`：质量报告、冲突/重复/缺口/公开范围问题、最小确认项和人工决策生成新版本的契约；
- `v2/src/modules/brand-truth/brand-truth-quality-repository.ts`：租户事务内质量报告持久化、读取和审计原子提交；
- `v2/migrations/0004_brand_truth_quality.sql`：质量报告外键、不可变保护、禁止清空、RLS 和租户索引；
- 冲突仅在相同结构化主体与属性出现不同陈述时形成候选；措辞规范化相同只标记重复，不使用未经验证的语义硬判；
- 缺少资料时只生成具体补充问题，不生成事实答案；公开范围不确定或命中敏感词时阻止自动公开并要求人工确认；
- 人工决定产生新的事实卡草稿版本，不覆盖原事实卡或质量报告。

## Gate D 本地验证证据

- 快速契约与架构测试：5 个测试文件、24 项通过；
- 首次完整验证发现并保留 2 类失败：数据库规则版本正则转义过度，以及新增外键后原清空测试断言过窄；
- 修复后补强人工决策持久化场景，最终完整隔离验证：11 个测试文件、55 项通过，0 失败、0 跳过；
- PostgreSQL 16.14 冷备恢复通过，Redis 7.2.16 AOF 进程终止恢复通过；
- 结果文件：`C:\Users\ADMINI~1\AppData\Local\Temp\answertravel-v2-test-DEF1jo\vitest-results.json`；
- GitHub Linux CI：提交 `e82d9c555917bdda30b2bfc1804e8c548e0a7233`，Actions 运行 `34794524583`；Ubuntu `verify` 作业成功；
- Linux CI 的 Node.js 24、PostgreSQL 16、Redis 7、`npm ci`、测试数据库迁移/角色初始化和 `npm run check:integration` 全部步骤为 `success`。

## 下一门禁

1. 向用户展示业务结果、失败边界和当前非目标；
2. 用户通过 Gate E 后更新决策日志并封版。
