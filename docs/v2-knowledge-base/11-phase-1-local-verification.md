# Phase 1 本地验证结果

日期：2026-09-13。状态：本地技术验证与 Linux CI 通过；阿里云实机受主机策略阻断；用户已确认 Phase 1 业务验收通过。

| 业务要求 | 实测结果 |
| --- | --- |
| 品牌数据不能串用 | 不同品牌空间相互不可见，越权写入失败 |
| 原始回答不能被改写 | 证据与审计的修改、删除和整表清空均被拒绝 |
| 重复任务不重复计算事实 | 并发重复请求和再次投递只保留同一证据与审计 |
| 每条证据都有操作记录 | 证据与审计同时提交；任一失败则回滚 |
| 临时故障可以恢复 | 任务失败后重试成功；不支持的任务版本明确失败 |
| 服务故障不能显示正常 | 就绪检查正确区分可用、不可用及危险数据库账号 |
| 备份可恢复 | 从数据库冷备份读回相同证据哈希和审计 |
| 队列重启不丢待处理任务 | 强制结束独立 Redis 进程后，从 AOF 恢复等待任务 |

共 33 项自动测试，33 通过、0 失败、0 跳过；另完成数据库和 Redis 两项恢复演练。全部测试使用隔离的明确测试数据，未接真实 AI、未调用用户 API Key、未改动线上业务。

原始结果及复现方法见 `08-phase-1-implementation-log.md` 和 `v2/README.md`。

## Linux CI 复核

- GitHub 仓库：`spigle760918-del/answertravel`；提交 `c2a802ce1db27eb186c6626df72d4bfdfb0c3e63`。
- `AnswerTravel V2 CI` 的 push 运行 `34753545909` 与手动运行 `34753580732` 均为 `success`。
- 手动运行的 Ubuntu `verify` 作业依次完成 PostgreSQL 16、Redis 7、Node.js 24、`npm ci`、测试角色初始化和 `npm run check:integration`，没有跳过或失败的工作流步骤。
- 远端 46 个 V2 受控文件与本地内容逐文件规范化比较一致；远端工作流只额外增加 `workflow_dispatch` 手动入口，不改变验证命令。
- GitHub 匿名接口不提供逐行日志下载；用户报告该运行显示 33 项全部通过，这与同一测试内容的本地 33/33 结果一致。CI 成功状态、步骤与提交已由 GitHub API 独立核验。

## 后续门禁

1. 阿里云现有业务服务器的原生测试被宝塔/主机安全策略阻断；不绕过安全层，也不把该路径记为通过。
2. Linux CI 已通过，满足 Phase 1 的跨平台工程验证门禁；这不等于生产部署、在线备份或外部告警验收。
3. 用户完成 Phase 1 最终业务确认后，才更新下一切片意图卡并进入品牌真相中心。
