# AnswerTravel V2 协作约束

本工作区正在设计 AnswerTravel V2。任何参与者在进行 V2 设计、编码、测试或评审前，必须先阅读：

1. `docs/v2-knowledge-base/README.md`
2. `docs/v2-knowledge-base/01-product-charter.md`
3. `docs/v2-knowledge-base/02-architecture-baseline.md`
4. `docs/v2-knowledge-base/03-vibe-coding-sop.md`
5. `docs/v2-knowledge-base/04-source-ledger.md`
6. `docs/v2-knowledge-base/05-decision-log.md`

## 当前门禁

- 用户已于 2026-09-13 明确确认：“以你确定的 V2 为准。继续”。
- V2 产品章程、总体架构、SOP、指标原则和 V1/V2 边界已生效。
- 当前仅允许按 `docs/v2-knowledge-base/07-phase-1-intent-card.md` 实施 Phase 1；扩大范围必须先更新意图卡或决策日志。
- V1 仅作为历史事实、可迁移资产和反例来源；不得把 V1 的模拟趋势、模拟竞品、模拟发布或模拟收录迁入 V2 生产路径。

## 已确认技术基线

- 主系统使用 TypeScript；运行时固定在受支持的 Node.js LTS，Phase 1 目标为 Node.js 24.x。
- Python 只用于确有必要的统计、研究或离线分析任务，不与主系统争夺业务事实源。
- 开发、测试、生产统一使用 PostgreSQL，不使用 SQLite 作为过渡业务数据库。
- 定时器只负责产生计划，任务状态必须进入持久化队列；不得以进程内定时器作为生产调度事实源。
- 官方 API 优先；网页/App 采样必须是独立终端适配器，并遵守授权、条款、速率和风控边界。
- Playwright 可用于获授权终端采样；不得使用反检测工具绕过验证码、访问控制或平台限制。

## 不可违反的产品原则

- AI 与自动化优先；人负责企业真相、差异化能力和高风险动作的最终确认。
- 所有指标必须可追溯至不可变的原始回答、采集上下文和来源证据。
- 不得伪造事实、引用、评论、排名、收录或效果归因。
- 排名、情感、引用吸收等无法可靠判断时必须返回 `不适用`、`不确定` 或 `证据不足`，不得硬算。
- 生产路径禁止使用未明确标注的 mock、演示或推导数据。
- 每个模块只承担一个明确业务职责；模块通过版本化契约协作。
- 每个垂直切片必须具备验收场景、自动化测试、可观察性、失败恢复和知识库更新。

## 防跑偏和防幻觉

- 新需求先写入产品章程或决策日志，再进入实现。
- 事实、推断、建议、假设必须明确分级。
- 外部项目能力只以已核验资料为依据；计划、示例和宣传不得当作已验证生产能力。
- 涉及外部代码复用时，先固定版本，再做许可证、安全、依赖和运行验证。
- 架构变化必须写 ADR；若改变已确认的产品边界，必须再次获得用户确认。
