import {createOptimizationActionPlan} from "./optimization-action-plan.js";import {OptimizationActionPlanRepository} from "./optimization-action-plan-repository.js";
export class OptimizationActionPlanService{constructor(private readonly repository:OptimizationActionPlanRepository){}async create(tenantId:string){const routing=await this.repository.latestRouting(tenantId);if(!routing)throw new Error("Evidence gap routing snapshot is required.");const draft=createOptimizationActionPlan(tenantId,routing);const existing=await this.repository.byHash(tenantId,draft.inputSha256);if(existing)return{plan:existing,idempotent:true};return{plan:await this.repository.save(draft),idempotent:false};}}
